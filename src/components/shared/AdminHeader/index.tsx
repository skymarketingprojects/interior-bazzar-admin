import styles from "./AdminHeader.module.css";
const AdminHeader = () => {
  return (
    <header className={styles.header}>
      {/* <h1 className={styles.title}>{user?.id} Dashboard</h1> */}
    </header>
  );
};
export default AdminHeader;
